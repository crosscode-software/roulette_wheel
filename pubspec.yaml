name: roulette_wheel_selection
description: > 
  A simple visual widget of roulette wheel selection.
version: 1.0.0
homepage: https://vocus.cc/@cross-code/61ee44dffd897800014cabb1
repository: https://github.com/crosscode-software/roulette_wheel_selection

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
    
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
 